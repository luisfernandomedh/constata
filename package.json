{
  "name": "constata",
  "version": "0.1.0",
  "description": "Detecta señales de estafa en mensajes y explica por qué. Núcleo determinista, sin dependencias, corre en navegador y en Node.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && rm -rf docs/lib && cp -R dist docs/lib",
    "test": "node --test test/*.test.js && node scripts/instrucciones.mjs --verificar",
    "medir": "node scripts/medir.mjs",
    "verificar": "node scripts/verificar-dominios.mjs",
    "verificar:json": "node scripts/verificar-dominios.mjs --json",
    "cobertura": "node scripts/cobertura.mjs",
    "cobertura:json": "node scripts/cobertura.mjs --json",
    "instrucciones": "node scripts/instrucciones.mjs"
  },
  "keywords": [
    "phishing",
    "smishing",
    "scam",
    "fraude",
    "estafa",
    "seguridad"
  ],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.10.0"
  }
}
